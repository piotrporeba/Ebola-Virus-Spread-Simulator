#include "infectionFunc.c"

/*
 * Visual stuff
 */
GtkWidget *drawArea;

/*
 * 800 with lower cell count meant larger cells
 * 1000 with [1000][1000] cells will be the size of pixels
 */
int windowSize = 800;
